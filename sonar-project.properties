# sonar-project.properties
# Cấu hình SonarCloud cho dự án ScrapTech (React Native / Expo)

# ── Định danh dự án ──────────────────────────────────────────────────────────
# projectKey = <organization>_<repository> (theo quy tắc SonarCloud)
sonar.projectKey=Flappy-Devs_ScrapTech
sonar.organization=flappy-devs

# Tên và phiên bản hiển thị trên SonarCloud dashboard
sonar.projectName=ScrapTech Mobile
sonar.projectVersion=1.0.0

# ── Nguồn mã nguồn ───────────────────────────────────────────────────────────
sonar.sources=app,src
sonar.tests=__tests__

# Các file/thư mục bỏ qua khi phân tích
sonar.exclusions=\
  **/node_modules/**,\
  **/__mocks__/**,\
  **/coverage/**,\
  **/*.d.ts,\
  **/assets/**,\
  **/android/**,\
  **/ios/**

sonar.test.inclusions=**/__tests__/**/*.test.tsx,**/__tests__/**/*.test.ts

# ── Ngôn ngữ & encoding ──────────────────────────────────────────────────────
sonar.sourceEncoding=UTF-8

# ── Báo cáo coverage (lcov từ Jest) ─────────────────────────────────────────
# File này được tạo khi chạy: npx jest --coverage
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Chỉ đo coverage cho 2 file được test thực sự:
#   - app/MyOrdersScreen.tsx       (được test bởi HomeScreen.test.tsx)
#   - app/Onboarding1Screen.tsx    (được test bởi OnboardingScreen.test.tsx)
# Tất cả file khác bị loại khỏi báo cáo coverage (không ảnh hưởng code quality analysis)
sonar.coverage.exclusions=\
  app/LoginScreen.tsx,\
  app/SplashScreen.tsx,\
  app/VerifyOtpScreen.tsx,\
  app/_layout.tsx,\
  app/(app)/**,\
  app/(auth)/**,\
  src/**

# ── TypeScript ────────────────────────────────────────────────────────────────
sonar.typescript.tsconfigPath=tsconfig.json
